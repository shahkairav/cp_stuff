/*
- a[i]: maximum number of characters starting from i that are also a prefix of the string.
- a[0] is generally not well-defined.
- Z function can be calculated in O(n).
*/

#include <vector>




int main() {
  return 0;
}