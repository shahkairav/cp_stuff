// Chinese Remainder Theorem

/*
x := a mod m 
  is equivalent to 
x := a1 mod m1
x := a2 mod m2 ...
x := a_k mod m_k

where (m1, m2, ..., m_k) are pairwise co-prime.

x has a unique solution upto modulo m = m1 * m2 * ... * m_k

*/

