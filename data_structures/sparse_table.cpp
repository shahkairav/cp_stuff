/*
- Allows answering range queries in O(log N)
- Its true power lies in answer range-maximum/minimum queries in O(1)
- However, does not support array updates.
*/

// Precompute all answers for queries of ranges of lengths that are powers of 2.

class SparseTable {
private:
  int K = 25; // reasonable value of log N for N <= 10^7

public:

};

int main() {
  return 0;
}